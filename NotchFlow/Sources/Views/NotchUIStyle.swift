import SwiftUI

enum NotchTheme {
    static let textSecondary = Color(white: 1.0).opacity(0.50)
}

enum NotchType {
    static let heroLarge = Font.system(size: 48, weight: .heavy, design: .rounded)
    static let caption = Font.system(size: 13, weight: .regular, design: .rounded)
    static let compactTime = Font.system(size: 14, weight: .medium, design: .rounded)
}
